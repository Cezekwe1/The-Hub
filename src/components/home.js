import React, { Component } from "react";

export default class home extends Component {
  render() {
    return (
      <div>
        <button>Login</button>
        <button>SignUp</button>
      </div>
    );
  }
}
